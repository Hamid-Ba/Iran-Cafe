# /etc/nginx/sites-available/cafesiran
# Initial configuration without SSL (before certbot)
server {
    listen 80;
    server_name api.cafesiran.ir;
    
    # Allow certbot to work
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    
    # Static files
    location /static/ {
        alias /home/cafesiran_back/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Media files
    location /media/ {
        alias /home/cafesiran_back/media/;
        expires 1y;
        add_header Cache-Control "public";
    }
    
    # WebSocket connections
    location /ws/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
    
    # Regular HTTP requests
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
}

# HTTPS configuration (will be added by certbot automatically)
# server {
#     listen 443 ssl http2;
#     server_name api.cafesiran.ir;
#     
#     # SSL certificates (will be generated by certbot)
#     ssl_certificate /etc/letsencrypt/live/api.cafesiran.ir/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.cafesiran.ir/privkey.pem;
#     
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     
#     # Security headers
#     add_header X-Frame-Options DENY;
#     add_header X-Content-Type-Options nosniff;
#     add_header X-XSS-Protection "1; mode=block";
#     
#     # Static files
#     location /static/ {
#         alias /home/cafesiran_back/static/;
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#     
#     # Media files
#     location /media/ {
#         alias /home/cafesiran_back/media/;
#         expires 1y;
#         add_header Cache-Control "public";
#     }
#     
#     # WebSocket connections
#     location /ws/ {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 86400;
#     }
#     
#     # Regular HTTP requests
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#     }
# }

# Optional: Flower monitoring (uncomment if needed)
# server {
#     listen 443 ssl http2;
#     server_name flower.cafesiran.ir;
#     
#     ssl_certificate /etc/letsencrypt/live/flower.cafesiran.ir/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/flower.cafesiran.ir/privkey.pem;
#     
#     # Restrict access (recommended for production)
#     allow YOUR_IP_ADDRESS;
#     deny all;
#     
#     location / {
#         proxy_pass http://127.0.0.1:5555;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }